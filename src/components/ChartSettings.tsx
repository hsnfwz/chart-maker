'use client';

import { useChartContext } from "@/hooks/context";

function ChartSettings() {
  const {
    color,
    setColor,
  } = useChartContext();

  return (
    <>

    </>
  );
}

export default ChartSettings;
